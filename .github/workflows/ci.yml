name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  # Deterministic, GPU-free — this is the gate that should block merges.
  build-and-unit:
    name: Typecheck · Build · Unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      - run: npm test

  # End-to-end against a real (software) WebGPU Chromium. WebGPU runs via SwiftShader on
  # the GPU-less runner (see playwright.config.ts flags) — confirmed working, so this is a
  # required gate.
  e2e:
    name: E2E (Playwright · software WebGPU)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Fetch Freedoom WAD (gitignored asset)
        run: npm run fetch-wad
      - name: Install Playwright Chromium + system deps
        run: npx playwright install --with-deps chromium
      - name: Run E2E
        run: npm run test:e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
